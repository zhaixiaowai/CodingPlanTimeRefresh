import 'package:flutter_test/flutter_test.dart';

/// 占位测试：Task 1 脚手架阶段。
/// 各业务模块（aes/sse/config/usage/scheduler 等）的单元测试
/// 将在对应任务（Task 2+）中按 TDD 添加。
void main() {
  test('scaffold placeholder', () {
    expect(1 + 1, 2);
  });
}
